{{ 'section-video-banner.css' | asset_url | stylesheet_tag }}
<div class="banner-text-wrapper">
  <div><h1 class="banner-heading-h1">{{ section.settings.banner-title }}</h1></div>
  
  <h3 class="banner-heading-h3">{{ section.settings.banner-subtitle }}</h3>
  <a class="button button-primary banner-button" href="#" target="blank">BOOK AN APPOINTMENT</a>
  <div class="circle1"></div>
  <div class="circle2"></div>
  <div class="circle3"></div>
  <p class="banner-text-small">SCROLL</p>
</div>
<div class="video-section-wrapper">
  
  <div class="frame-container">
    <img class="bannerImg" src="{{section.settings.banner-image1| img_url: '1440x960' }}" style="width:100%;height: 100%;object-fit: cover;">
    <img class="bannerImg" src="{{section.settings.banner-image2| img_url: '1440x960' }}" style="width:100%;height: 100%;object-fit: cover;"">
    <img class="bannerImg" src="{{section.settings.banner-image3| img_url: '1440x960' }}" style="width:100%;height: 100%;object-fit: cover;"">
    <!-- <video id="bannerVid" muted autoplay loop>
      <source src="{{'walkIn.mp4' | asset_url }}" type="video/mp4" >
    </video>     -->
  </div>
  
</div>
<script>
  var myIndex = 0;
carousel();

function carousel() {
  var i;
  var x = document.getElementsByClassName("bannerImg");
  for (i = 0; i < x.length; i++) {
    x[i].style.display = "none";  
  }
  myIndex++;
  // console.log(x.length)
  if (myIndex > x.length) {myIndex = 1}    
  x[myIndex-1].style.display = "block";  
  setTimeout(carousel, 2000); // Change image every 2 seconds
}
</script>
{% schema %}
  {
    "name": "Banner",
    "class":"overflow-hidden",
    "settings": [
      {
        "id": "banner-title",
        "type": "text",
        "label": "Banner title",
        "default":"GREAT CUTS. BIG SMILES."
      },
      {
        "id": "banner-subtitle",
        "type": "text",
        "label": "Banner subtitle",
        "default":"Look and feel like your best self."
      },
      {
        "id": "banner-image1",
        "type": "image_picker",
        "label": "Banner image slider 1st image"
        
      },
      {
        "id": "banner-image2",
        "type": "image_picker",
        "label": "Banner image slider 2nd image"
        
      },
      {
        "id": "banner-image3",
        "type": "image_picker",
        "label": "Banner image slider 3rd image"
        
      }
    ],
    "presets": [
      {
        "name": "Banner",
        "category": "Video"
      }
    ]
  }
{% endschema %}